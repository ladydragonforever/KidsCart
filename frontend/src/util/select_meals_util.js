export const fetchSelecteMeals = childId => {
   return axios.get('/api/')
};

export const deleteSelectMeal = selectMealId => {
   return axios.get('/api/')
};

export const addSelectMeal = (mealId, childId) => {
   return axios.get('/api/')
};